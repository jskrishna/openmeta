# `@openmeta/sdk` (working name)

Public SDK for building on OpenMeta (extensions, integrations, custom fields).

## Planned

- Stable TypeScript / PHP client surfaces
- Types for models and fields
- Extension registration helpers
