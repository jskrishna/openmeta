# `@openmeta/ui`

> Shared UI tokens, primitives, and components for Admin / Builder.

**Status:** ✅ Complete (Phase 9 / `v0.8.0-alpha`) · PHP view components (React kit later)  
**Blueprint:** [SPEC.md](./SPEC.md)

## Surface

- Tokens / Theme
- Primitives: `Button`, `Input`, `Notice`
- Components: `Card`, `Form`, `DataTable`

```bash
composer test:ui
```
