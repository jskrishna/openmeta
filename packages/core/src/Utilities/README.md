# Utilities/

General-purpose utilities for the core package. Prefer `Support/` for array/string helpers used by runtime; keep this folder for broader utilities as they appear.

No database, fields, REST, GraphQL, admin, or builder utilities belong here.
