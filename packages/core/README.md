# `@openmeta/core` (working name)

Core content modeling engine and WordPress plugin bootstrap.

## Planned

- Content models and field registry
- Storage adapters
- REST surface (baseline)
- Extension hooks

See `docs/architecture/` and `docs/fields/` as design notes land.
