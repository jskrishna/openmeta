# Core package tests

Architecture placeholder for `packages/core` tests.

```text
tests/
├── Unit/           # No WP bootstrap
└── Integration/    # WP / plugin boot (later)
```

Mirror `src/` namespaces when PHPUnit lands (Phase 10 / as v0.1 code appears).

See [docs/testing/](../../../docs/testing/) and root [tests/](../../../tests/).
