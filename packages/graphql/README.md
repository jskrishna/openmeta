# `@openmeta/graphql` (working name)

First-class GraphQL support for OpenMeta models and fields.

## Planned

- Schema generation from content models
- Resolvers and mutations
- Auth / capability mapping
