# Website docs

Docs portal for openmeta.dev (or equivalent). Prefer sourcing content from the repo-root `docs/` tree to avoid drift.

## Planned

- Static site / MDX pipeline
- Search
- Versioned docs (when releases exist)
