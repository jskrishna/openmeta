{
  "name": "openmeta",
  "version": "0.0.0",
  "private": true,
  "description": "Modern open-source content modeling framework for WordPress.",
  "license": "GPL-2.0-or-later",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "website",
    "examples/*",
    "tools/*"
  ],
  "scripts": {
    "lint": "echo \"Frontend lint pipeline not configured yet — see TECH_STACK.md\"",
    "build": "echo \"Frontend build pipeline not configured yet — see TECH_STACK.md\"",
    "test": "echo \"Frontend tests not configured yet — see tests/README.md\""
  },
  "devDependencies": {}
}
