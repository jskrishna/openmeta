---
title: "{{ Package }} API Overview"
description: "Public API surface of the {{ package-slug }} package."
package: "{{ package-slug }}"
category: api
version: next
status: draft
last_updated: "{{ YYYY-MM-DD }}"
tags: ["{{ package-slug }}", "api"]
---

# {{ Package }} API Overview

Public entry points of `openmeta/{{ package-slug }}`. Full generated reference
lives under the [reference](../../reference/README.md) section.

## Public contracts

- `{{ Interface }}` — {{ purpose }}

## Façade

```php
// {{ Resolve and use the package façade from the container. }}
```

## Extension points

- {{ How third parties extend this package. }}
