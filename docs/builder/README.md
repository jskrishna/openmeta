# Visual Builder

The low-code visual configuration engine — canvas, schema, history, templates,
and conditions — orchestrating Fields, Validation, and Security. Architecture
only (no frontend UI).

Full reference: [`packages/builder`](../../packages/builder/README.md).
