# REST

The REST layer provides a framework-independent Router, Request/Response,
Middleware, and Resources, with an application surface in the `api` package and a
WordPress mount in the adapter.

Full reference: [`packages/rest`](../../packages/rest/README.md) ·
[`packages/api`](../../packages/api/README.md).
